services:
  - type: web
    name: gtube-api
    runtime: python
    rootDir: .
    buildCommand: pip install -r requirements-cloud.txt && python -m pip install --upgrade yt-dlp && which yt-dlp
    startCommand: python -m backend.server --production
    envVars:
      - key: CORS_ORIGIN
        sync: false
